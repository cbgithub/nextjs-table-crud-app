import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /*async redirects() {
    return [
      {
        source: '/', // The original path the user requests (e.g., the home page)
        destination: '/', // The page you want to redirect to
        permanent: true // Use 308 (permanent redirect) or false for 307 (temporary redirect)
      },
    ];
  },*/
};

export default nextConfig;
